# Архитектурен документ · MasterBook

**Дата:** 2026-08-22 · **Статус:** жив (Етап 1 нов) · **Аудитория:** разработчик/агент, който пише кода
**Стъпва върху:** файл 3 (скелетът) · masterbook-data (модел на данните) · Плана за реакция (устойчивост)

Този документ е входната точка за строежа. Дребните детайли (полета, формули, инциденти) са в свързаните файлове — тук е *как е устроено цялото и защо*. Не се дублира; връзва се.

## 1 · Контекст и цели

MasterBook управлява имоти, наеми, продажби, кредити, финанси, проекти и Гант за собственик с интензивен график. Целта на кода е **малък, бърз и дългосрочно безпроблемен** — малко бъгове, **нула загуба на функционалност**, и над всичко **нула загуба на данни**. Последното не е лозунг: цялата архитектура е избрана, за да прави загубата на данни структурно невъзможна, а не въпрос на дисциплина.

Един собственик, интензивен живот — значи софтуер, който записва вместо да помни, показва веднага вместо да чака, и никога не превръща поправимото в непоправимо.

## 2 · Общ дизайн · event-sourcing (CQRS)

Истината живее само в **Журнала** — дневник от събития, само добавяне, хеш-верига, opId. Всичко останало е **производно**: то се изчислява от Журнала и не се съхранява. Четенето и писането са разделени (CQRS): пишеш през една Врата, четеш от много Огледала.

```
   ПОТРЕБИТЕЛ
      │  редактира (оптимистично, местно)
      ▼
  ┌─ ПЯСЪЧНИК ─┐   IndexedDB · записът Е местен и мигновен · постоянно хранилище
  └─────┬──────┘
        │ потвърждава
        ▼
     ВРАТАТА ──────► проверява право + валидност (ЕДИНСТВЕНИЯТ вход за запис)
        │
        ▼ пише СЪБИТИЕ (append + хеш + opId)
  ╔═ ЖУРНАЛЪТ ═╗   само добавяне · хеш-верига · единствената истина · НИКОГА презапис
  ╚═════┬══════╝
        │ чете се
        ▼
   ОГЛЕДАЛОТО ────► производно състояние · НЕ се пази · преизгражда се · показва се
        │
        ▼ (навън)
      МОСТЪТ ──────► архив за Ексел (5 листа с филтри) + JSON износ↔внос
                     идемпотентно по opId · по поискване, не непрекъснат поток

   поправка = НОВО събитие (сторно) ──► обратно във ВРАТАТА → ЖУРНАЛА
```

Кръгът е седем стъпки — действие в Пясъчника, потвърждение през Вратата, запис в Журнала, преизграждане на Огледалото, поправка чрез сторно, синхрон през Моста, разрешаване на конфликт с rev-предпазител. Пълните стъпки са във файл 3.

Следствие за всеки, който пише заявки: **„текущо състояние" е агрегат на всички събития за същността**, не последен ред. Това е най-честата грешка и коренът на инцидента със загубата при пресяването.

## 3 · Десетте контейнера

| контейнер | роля |
| :---- | :---- |
| Обвивка | табове и изгледи; сама не пази истина |
| ВРАТАТА | единственият запис; проверява право и валидност |
| ЖУРНАЛЪТ | единствената истина; само добавяне, хеш-верига, opId |
| Огледалото | производно състояние; чете се, не се пази |
| Пясъчникът | местно-първо (IndexedDB) — записът Е местен и мигновен, затова „оптимистична редакция" и „бутон СЕГА" отпаднаха: няма сървър, който да се чака |
| Смятачът | пари в цели стотинки; ДДС по държава/сектор (два акумулатора) |
| Рисувачът | grid/Гант/форма; Гантът е ТАБЛИЦА с оцветени клетки, не диаграма |
| Правата | вход БЕЗ ПАРОЛА през Google/Microsoft/Apple; роли при ПОВЕЧЕ от един акаунт (ADR-007) |
| Мостът | ПОСТРОЕН (ADR-002): архив за Ексел с филтри + JSON износ↔внос със сверка |
| Хранилището | drive.file + Picker; клиентът връзва свой драйв |

Развитието на всеки контейнер е кодът — този документ дава границите му, не имплементацията.

## 4 · Модел на данните

Пълният модел (същности, ключове, връзки, метрики, стандартни филтри, капани) е в отделния masterbook-data skill. Накратко: портфейл → имот → единица → (наем ИЛИ продажба); наемателят поражда вземания, вземанията се погасяват с плащания; делата съдържат задачи с Гант и критичен път. Всяка тяхна промяна е събитие през Вратата в Журнала.

**Не дублирай тук** — при промяна на модела се редактира skill-ът, а този документ само сочи към него.

## 5 · Ключови решения и компромиси

**Event-sourcing вместо „таблица със състояние".** Нищо не се губи; всяка промяна
е проследима; поправката е сторно, не презапис. Цената: четенето иска агрегат, не
един ред — **и тя е измерена.** Цяло Огледало от 10 000 събития се смята за
милисекунди (`docs/09` §4б). Затова планираните отметки/checkpoints **ОТПАДАТ**:
решаваха проблем, който при този мащаб няма.

**ЕДИН двигател.** В началото бяха замислени два — табличен и формулен. Слети са
в един event-sourcing двигател: таблицата и формулата са ИЗГЛЕДИ (Огледало), не
отделни машини. Втори двигател не се връща; за голям клиент се сменя НОСИТЕЛЯТ,
а Журнал-договорът (`opId` + хеш) остава един.

**Носителят е РЕШЕН — В · местно-първо** (ADR-001; доказан с мерки, `docs/09` §4б).
Журнал-договорът (opId + хеш) работи еднакво на всеки носител — портовете имат
по две реализации, затова сървър за платените планове се добавя без
пренаписване.

**Пари в цели стотинки.** Никога float — иначе стотинкови разминавания в ДДС. ДДС в отделни акумулатори по държава/сектор.

**Самоличност = вход БЕЗ ПАРОЛА.** (Сверено 22.08, **ADR-004**; заменя по-ранното
твърдение за споделен акаунт.) Влиза се през Google, Microsoft или Apple, или с
passkey на машината. Приложението никога не вижда парола: няма своя, няма
възстановяване, няма какво да изтече. Хранилището е на клиента, при неговия
доставчик.

**Достъпът на други хора минава през ДОСТАВЧИКА.** При ПОВЕЧЕ от един акаунт се
добавят други имейли — през тяхното споделяне и тяхната защита. Ние не каним
хора, не пазим чужди пароли и не отнемаме достъп; виждаме онзи, когото
доставчикът е пуснал, и записваме имейла му като `actor`. Ролите (собственик ·
редактира · наблюдава) и колонното право казват какво може човек В ПРИЛОЖЕНИЕТО.
В **Личния** план ролите ги няма изобщо — той е един акаунт по замисъл.
Изолация на наемател (tenant) на ниво данни — винаги.

**Гант = оцветена таблица, не диаграма.** По-малко код, по-бърз рендер, същата информация.

## 6 · Поток на данните и интеграции

Потребителят пише в **Пясъчника** — записът е МЕСТЕН и мигновен (< 20 ms), затова
няма „оптимистично" чакане на сървър. Минава през **Вратата**, която пише събитие
в **Журнала**. **Огледалото** се преизгражда при всяко показване (мерките:
`docs/09` §4б) и се
показва — а огледалата са няколко, всяко под свой ъгъл.

Навън **Мостът** е построен като **архив за Ексел** (5 листа
с готови филтри) и **JSON износ ↔ внасяне** със сверка на веригата преди запис.
И двата излизат само през право (`mozheDaIznasya`).

**Хранилището** е Драйвът на клиента: файловете се **ЧЕТАТ**, не се качват — в
Журнала влиза само следата (име, час, sha256 на прочетените байтове). Drive не е
втора истина.

Точка на внимание при синхрона: **курсор на наемател, не общ.** Общ курсор прескача събития между наематели — тих инцидент без симптом (виж плана за реакция, №4).

## 7 · Устойчивост · какво трябва да съществува преди първия инцидент

Пълните седем начина на счупване и първите действия са в **Плана за реакция**.
Трите неща, които трябваше да ги има от старта:

1. **Спирателен кран за Вратата** — ✅ построен (`vrata.zatvori`), доказан в
   прохода: скъсана верига и несъвпаднала котва го дърпат сами.
2. **Проверка на веригата по разписание** — построена е като бутон „Провери
   веригата" плюс автоматична проверка на **котвата** при всяко тръгване
   (`src/yadro/kotva.ts`). Часовникът не е построен; докъде е стигнало, стои в
   честния списък — `docs/09` §3, неговият единствен дом.
3. **Сверка вход↔изход при всяко преместване** — ✅ построена навсякъде:
   миграция, начисляване, сметки, актуализация от източник, внасяне. Точно
   липсата на тази сверка скри мълчаливо част от текста при пресяването на
   извора; колко точно — печатът е в `00-CHETI-PARVO.md` §2.

Първото правило над всички: **при инцидент Журналът не се пипа.** Поправката е ново събитие.

## 7а · Продуктите · МАТРИЦА от два критерия (ADR-007)

**Няма безплатен ПЛАН — безплатен е АКАУНТЪТ.** Един двигател, **два критерия**,
като ТАБЛИЦА от възможности (`src/domein/planove.ts`), не разклонена база:

|  | **1 акаунт** | **повече акаунти** |
| :---- | :---- | :---- |
| **локално** | Личен · локално | Професионален · локално |
| **онлайн** | Личен | **Професионален** ← стартъпът |

- **Първи критерий · КЪДЕ Е ИНФОРМАЦИЯТА.** Локално значи без облак: Журналът е
  само на устройството, и износът става задължение, защото няма автоматично копие.
- **Втори критерий · КОЛКО АКАУНТА.** Оттук идва колонното право: при един акаунт
  въпросът „кой вижда коя колона" не съществува.

**ВСЯКО издание работи офлайн** — свойство на двигателя, не на план
(`rabotiOflayn()` връща `true` за всички). Офлайн изданието се различава по едно:
свързващата част ЛИПСВА в него, а не е изключена.

**Нагоре не се добавят ФУНКЦИИ, а КАПАЦИТЕТ** — място и сигурност, купени от
Google, Microsoft или Apple.

**Изборът е двуслоен**: планът ПОЗВОЛЯВА ли · и собственикът я е ОТМЕТНАЛ ли.
Отметките са на **Таблото** и живеят в localStorage, не в Журнала — кой бутон е
бил скрит не е факт от историята на парите. Кодът пита възможността по име,
никога плана.

## 8 · Отворени въпроси

- **Носителят** — РЕШЕН (ADR-001; мерките в `docs/09` §4б): местно-първо в IndexedDB,
  портове с по две реализации, сървър се добавя без пренаписване.
- **Втори двигател** — ОТПАДА като идея: продуктите са ТАБЛИЦА от възможности
  над същия двигател, не втора машина. За голям клиент се добавя сървър като
  НОСИТЕЛ, не като втори двигател — Журнал-договорът остава един.
- **Проследимост на „доклад"-източниците** — част от историческите данни нямат адрес; маркират се непроверими.

---

*Архитектурен документ · 22.08.2026. Свързани: `ИЗВОР-3-ЗАДАНИЕ` (скелетът) ·
`.claude/skills/masterbook-data` (моделът на данните). При разминаване — те са
по-конкретни; тук е цялото. Устойчивостта живее в кода: спирателният кран,
котвата и курсорът на наемател, всеки със свой тест.*
