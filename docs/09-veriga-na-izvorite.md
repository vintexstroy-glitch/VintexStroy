# ВЕРИГАТА НА ИЗВОРИТЕ · от думата до теста

**Дата:** 2026-08-22 · **Защо съществува:** за да не се пита втори път
„решихме ли това" и да не се повтаря казаното.

Този документ е **картата**: всяка дума на собственика — къде е записана, в кое
решение е превърната, къде живее като код и кой тест я пази. Ако нещо липсва в
дясната колона, значи е обещание, не функция.

---

## 1 · Слоевете и посоката

```
ДУМА (Драйв · извори)  ──►  РЕШЕНИЕ (docs/ · ADR)  ──►  КОД (src/ app/)  ──►  ТЕСТ + ПРОХОД
   само четене              живо, сверява се           истината            доказателството
```

**Едно правило държи веригата честна:** документ, който твърди нещо, което няма
тест, е бележка — не спецификация. Затова дясната колона долу е задължителна.

---

## 2 · Картата · дума → решение → код → тест

| дума на собственика | решение | код | тест |
| :---- | :---- | :---- | :---- |
| „нищо не се трие, поправка = сторно" | Журнал само за добавяне | `src/yadro/dnevnik.ts` · `vrata.ts` | `inv-1` · `inv-8` · `furtuna` |
| „парите са цели стотинки" | никакъв float; `_st` | `src/yadro/pari.ts` | `pari` · `furtuna` (23 вида повреда) |
| „всичко минава през едно място" | Вратата е единственият вход | `src/yadro/vrata.ts` | `vrata` · `inv-2` |
| „да си личи, ако някой пипне" | хеш-верига + **котва** | `hash.ts` · `kotva.ts` | `inv-1` · `sriv` · проход §13 |
| „при инцидент не се пипа" | спирателен кран | `vrata.zatvori` | проход §12 |
| „сверка вход↔изход, дори нула" | всяка партида завършва със сверка | `sverka.ts` + всички партиди | `inv-3` · `smetki` · `vnos` |
| „всички цени са с ДДС, не се разделя" | ДДС се ИЗВАЖДА от общата цена | `src/domein/dds.ts` | `dds` (инвариант: основа+ДДС=обща) |
| „ДДС на отделен ред в Сметки" | акумулатори по държава/сектор | `src/domein/smetki.ts` | `smetki` · `razhodi` |
| „ДДС от Приходи: Наеми, КЕШ, БАНКА, Заплати, Кредити" | таблица `POTOTSI` | `smetki.ts` | `razhodi` |
| „не може да се коригира, ако има справка" | **справката заключва периода** | `src/domein/zamrazyavane.ts` | `zamrazyavane` (7) · проход §14 |
| „внесеното на ръка, разликата да свети" | трите числа: изчислено ↔ декларирано ↔ платено | `app/smetki.ts` · `ДДСПлатено` | `zamrazyavane` · проход §14 |
| „корекцията е през таблицата → сверена промяна" | единственият вход в заключен период | `aktualizatsiya.ts` (`svereno`) | `zamrazyavane` |
| „файлът да се ЧЕТЕ, не да се качва" | снимка + отпечатък; файлът остава в Драйва | `src/iztochnik/snimka.ts` | `aktualizatsiya` · проход §11г |
| „ексел, пдф и CSV" | три четеца, **нула библиотеки** | `iztochnik/xlsx.ts` · `pdf.ts` · `csv.ts` | `iztochnik` (16) |
| „сваля се от хората с разрешение" | `mozheDaIznasya` в порта | `src/yadro/pravata.ts` · `app/main.ts` | — (в §3: списъкът чака истинския OAuth) |
| „архив пак в ексел, с филтри" | писач на .xlsx с AutoFilter | `src/iznos/excel.ts` · `app/arhiv.ts` | `excel` (кръгът: писач↔четец) |
| „фините филтри на Уиндоус" | стрелка · отметки · групи по дата/сума | `app/filtri.ts` | проход §14 |
| „няма безплатен ПЛАН — безплатен е АКАУНТЪТ" | акаунтът при доставчика е входът; планът мери капацитет | `src/domein/planove.ts` | `planove` (15) |
| „влизаш БЕЗ ПАРОЛА… даваш достъп до хранилище" | портът `VhodBezParola` · П3 затворен | `src/yadro/samolichnost.ts` | `samolichnost` (12) |
| „Персоналният е САМО един акаунт" | Личният без други имейли и без роли | `planove.ts` · таблицата | `planove` („стълбата се мери ПО ЛИНИЯ") |
| „сигурността за това не е в нас" | достъпът минава през доставчика | `samolichnost.ts` (документирано решение) | — (решение, не механизъм) |
| „избор с ОТМЕТКИ на функционалностите" | двуслойният `mozhe(izbor, v)` | `app/tablo.ts` · `app/main.ts` | `planove` · проход §15 |
| „ИИ — не вграден, а с опция да се свърже" | име в таблицата + етикет „скоро" | `OSHTE_NE_E_ZAPOCHNATO` | `planove` · проход §15 |
| „още огледала" | по имот · по контрагент | `src/ogledalo/izgledi.ts` | `izgledi` (сборовете затварят) |
| „изнесеното да може да се върне" | внасяне с проверка на веригата | `src/domein/vnos.ts` | `vnos` (9) |
| „бързина и дълга надеждност" | мерки под договор с бюджети | `proba/merki.test.ts` | `npm run merki` · 5/5 |
| „да живее собствен живот, без интернет" | служебен работник · кеш-първо · без автопрезареждане | `app/public/sw.js` · `stroezh/pechat-sw.mjs` | проход §16 (мрежата се къса наистина) |
| „2 критерия: къде е информацията · колко акаунта" | матрица 2×2, не стълба (ADR-007) | `src/domein/planove.ts` | `planove` (15) |
| „онлайн акаунтите пак трябва да работят офлайн" | `rabotiOflayn()` връща `true` за ВСЕКИ план | `planove.ts` | `planove` („обещание, не свойство") |
| „избор на азбуки при сваляне, по региони" | пакети `bg` · `evropa` · `plus` | `src/domein/azbuki.ts` | проход §16 (джобът пази своя пакет) |
| „в експлорера има превод автоматичен" | данните НЕ се превеждат; етикетите — да | `translate="no"` · 102 места | проход §17 |
| „езикът на интерфейса е на служителя; терминалът се заковава" | две решения, различни хора · езикът НЕ е право | `src/domein/ezitsi.ts` | `ezitsi` (5) |
| „нищо чуждо в пакета" (правило 10) | шрифтовете слизат от Google в хранилището | `app/shrifty/` · `app/stil.css` | проход §16 · CSP без изключения |
| „последните думи по тема заменят старата" | два слоя: чист извор (журнал) · по теми (огледало) | `docs/izvori/` | сверка вход↔изход във всяка секция на `01-chist-dopiska.md` |
| „извади написаното по пълни изречения… и ги прехвърли в Чистия Извор" | И1–И90, пренесени дословно | `docs/izvori/01-chist-dopiska.md` | всяко сверено срещу най-дългия си запис |
| „извлечението — индивидуално, чете и създава модела вътре" | картата на хедъра, записана в Журнала като `МоделЗаписан` | `src/iztochnik/model.ts` · `app/iztochnitsi.ts` | `chetets` (16) · проход §18 |
| „таблиците са всевъзможни" | отпечатък на ХЕДЪРА, не име на файл — вторият файл минава сам | `model.ts` (`poznavaLi`) | `chetets` („модел с друга глава не се прилага") |
| „ДДС е избор при въвеждане на всяка фактура" | ставката слиза на РЕДА; секторът само предлага | `dds.ts` (`stavkaNaReda`) · `razchitane.ts` | `dds` (15) · `chetets` · проход §18 |
| „веднага се хваща липсата и се намира по извлеченията" | трите ъгъла, сдвоени по номер на документ | `src/domein/sverka-dds.ts` | `sverka-dds` (8) · `chetets` · проход §18 |
| „информиран избор с информиран текст" за паролата | правило 13 се СТЕСНЯВА: парола само с изричен избор | `src/yadro/samolichnost.ts` | `samolichnost` (12) |
| „бутоните са модели на пътища" | бутонът в Журнала: папка · действие · посока · позволени модели | `src/domein/butoni.ts` · `app/nastroyki.ts` | `butoni` (20) · проход §19 |
| „едното е само сверяване, другото е записване — не може и двете" | посоката се ВАДИ от действието; бутон, който чете, няма път към писане | `butoni.ts` (`posokaNa`) | `butoni` („посоката е ЕДНА") |
| „Гант = решетката с времеви колони (★ −60)" *(р84·[28])* | седми екран: три колони с филтри, четири такта, първата колона е ДНЕС | `src/domein/gant.ts` · `app/gant.ts` | `gant` (24) · проход §24 |
| „когато разработим… **диаграмата му**, чак тогава ще решим" (И56) | двата изгледа стоят един до друг и се сменят с бутон — за да се сравнят с очи | `app/gant-diagrama.ts` | проход §24 („диаграмата се появи") |
| „1 седмица преди… жъл цвят… 2 дниу свети в червено" *(р59·[71])* | светофарът е ФУНКЦИЯ с неговите два прага, не палитра | `dela.ts` (`svetofar`) | `gant` („жълто ТОЧНО от седмия ден") |
| „не можеш да го направиш това забрана" *(р83·[35])* | лентите не се влачат; срокът се мени от полето, за да остане следа | `app/gant.ts` | проход §24 („лентата не се влачи") |
| „Имотите не се сгъват, сгъват се само делата и поддела" (И88) | Мястото няма сгъвач — то е колона; сгъването крие и подподделата | `dela.ts` (`vidimi`) | `gant` („не оставя сираци") |
| „ще правиш полета в Секция Отчети… с формули между всички таблици" (И90) | всяко число носи съставките си и адреса им; непълното КАЗВА какво чака | `src/domein/otcheti.ts` · `app/smetki.ts` | `otcheti` (17) · проход §23 |
| „Активи минус задължения" (И87) | Капитал ≠ Средства — две числа, две имена; сверка по два независими пътя | `otcheti.ts` (`kapital`) | `otcheti` („Капиталът и Средствата са различни числа") |
| „Комбинация — ръчно начало + автоматични движения" *(р48·[71])* | ново събитие `СалдоЗаписано`; в Журнала влиза САМО началото | `sabitiya.ts` · `deystviya.ts` | `otcheti` („повторният запис ПОПРАВЯ") |
| „Вземанията са Плащания по незавършили сделки… на Акт 16" (И90) | два вида вземане, които не се сливат; продажбеното чака M03 и го казва | `otcheti.ts` (`vzemaniya`) | `otcheti` („НЕ крие празната половина") |
| „бутонът не бива да приеме чужд файл" | списък от позволени модели · отказва НА ГЛАС с името на модела | `butoni.ts` (`modelZaTablitsata`) | `butoni` · проход §19 |
| „те не са бутон, а УМЕНИЕ НА ДАННИТЕ" | всяка числова колона се появява сама със знак /+/ или /−/ | `src/domein/chisla.ts` | `chisla` (14) · проход §19 |
| „сменя мястото си автоматично… или просто се изключва" | знакът се СМЯТА, изключването се ЗАПИСВА | `chisla.ts` · `model.izklyucheni` | `chisla` · `butoni` (А→Б→А) |
| „ако има разлика, се регистрира промяната в Журнала" | `СверкаЗаписана` · и когато е нула | `src/domein/sveryavane.ts` | `butoni` · проход §19 |
| „всяка таблица със своя папка" | папката е ЛОГИЧЕСКА: име в Журнала, не директория | `butoni.ts` (`papki`, `vPapka`) | `butoni` („папките се получават от бутоните") |
| „нищо не е константа" (трети закон на скелета) | Настройки са екран (M16); знакът е пътят за секторите | `app/nastroyki.ts` · `chisla.ts` | проход §19 · довършването е в §3 |

---

## 3 · Обещанията без тест (честният списък)

| какво | защо още няма тест |
| :---- | :---- |
| **правото за сваляне** | портът стои и се вика, но политиката е `VsichkoRazresheno` — истински списък има смисъл чак с истинския OAuth |
| **истинският OAuth** | иска регистрация при тримата доставчици и публичен адрес; портът и тестовете му стоят готови |
| **проверка на веригата по разписание** | днес е бутон + автоматична проверка на котвата при тръгване; часовникът не е построен |
| **курсор на наемател при синхрон** | няма сървър — правилото е записано за деня, в който дойде |
| **деветте непостроени пътя** | обявени поименно в `DEYSTVIYA` и показани като „обявен" в Настройки; тест пази само че бутон с непостроено действие се ОТКАЗВА |
| **знакът до Сметки** | числовите колони носят знак и се помнят изключени, но в Журнала влиза колоната с роля „сума". Пътят на останалите е следващ резен — казано е и на екрана |
| **видимостта на БУТОН по служител** | `vidimost` се записва и `vidimiButoni` го чете, но формата в Настройки още няма поле за него — колонното право мина първо |
| **„сигурността за това не е в нас"** | решение, не механизъм: достъпът се дава в Драйва. Служителят вече се ЗАПИСВА (ADR-011), но поканата и отнемането остават при доставчика |
| **`AKUMULATORI` и `POTOTSI` още са заковани** | третият закон („нищо не е константа") е изпълнен наполовина: знакът е пътят, но секторите и потоците още живеят в кода |
| **езиците · 495-те низа** | решението е взето (ADR-008) и `ezitsi.ts` го носи; таблицата с низовете не е построена — затова езиците НЕ се броят за готов резен |
| **колонното право · личното и глобалното ниво** | наложеното (главен акаунт → служител) е ПОСТРОЕНО (ADR-011, проход §20). Личното скриване „като ширините" и глобалното триене от всички таблици — не |
| **Редакторът на хедъри · Опис на Подредба** | цялата тема е събрана в `docs/izvori/03`; построено е само правото. Създаване и триене на колони, семейства хедъри, трите вида номенклатура — не |
| **Гант и Отчети** | решено е КАКВО (десктоп диаграма · телефон и HTML оцветена таблица · Отчетите в Сметки), написано в `docs/03`; нула код |
| **Калкулаторът** | проучването е готово (`docs/otcheti/kalkulator-metodologii.md`); числата в матрицата чакат него |
| **седем от тринайсет връзки в картата** | обявени поименно в `src/domein/vrazki.ts` и броени от `dokade()`: сигналът за преглед Сметки→Управление · бутонът „връщане към настоящо" · Продажби Архив · Калкулаторът (две) · номенклатурата на Контакти · поканата по имейл |
| **прозорците на таблото** | единайсетте му таба срещу нашите шест екрана — картата ги слага един до друг (`docs/izvori/04` §2); съвпадения по име: нула; преименуване не е правено |
| **пет реда, покрити САМО от прохода** | фините филтри · спирателният кран · офлайн джобът · азбуките · `translate="no"`. Проходът вече върви и в CI, но модулен тест зад тях няма |

Всичко останало в §2 има ред в дясната колона.

**Обратната посока — тестове без дума в §2:** `model` · `nachislyavane` ·
`nositel-indexeddb` · `popravka` · `migratsiya` · `rezen-1`. Те пазят построеното
от резени 1–8, за което няма отделно негово изречение — историята им е в
`docs/izvori/02-po-temi.md`.

---

## 4 · Числата · ЕДИНСТВЕНИЯТ им дом

> **Договор.** Числата на кода и на мерките живеят **само тук**. Никой друг
> файл не ги преписва — препраща към този раздел. Преписаното число ражда
> разминаване: точно така „132 проверки" оцеля на три места, докато истината
> вече беше 152.

### 4а · Какво е построено (24.08, след втората партида находки)

| мярка | стойност |
| :---- | ---: |
| резени в ръцете на собственика | **23 от 31** (1–18 · 14б картата · 14в валутата · 14г Редакторът · А джобът · Б матрицата) |
| **екрани** | **7** · Имоти · Пари · Стойност на Състояние · **Управление** · Сметки · Настройки · Табло |
| тестови файлове · тестове | 46 · **547** |
| проверки в истински браузър (проход) | **329** |
| редове код (`src` + `app`) | **18 189** |
| редове тестове (+ проход, мерки, строеж) | **8 719** (+ 2 080) |
| зависимости в готовия пакет | **0** |
| код · gzip | **84,1 KB** (77,1 js · 5,8 css · 1,2 html) |
| построени пътища от десетте | **2 / 10** (сверяване от Ексел · създаване на таблица) |
| издание | **Стартъп Алфа** (`src/izdanie.ts`) |
| азбуки в хранилището | **441,2 KB** · всички подмножества, ЦЕЛИ |
| джобът · пакет `bg` (по подразбиране) | 204,1 KB азбуки + 308,7 KB черупка |
| джобът · `evropa` · `plus` | 342,6 KB · 441,2 KB азбуки |
| контейнери от Заданието, изброени в плана | **19 / 19** (бяха 13) |

**Ред тест на всеки два реда код** (8 719 : 18 189 ≈ 0,48). За система, чието
първо правило е „нула загуба на данни", това е правилното съотношение — не украса.

**За азбуките, честно и с двете поправки:** първо оцених 50–70 KB — сгреших,
беше 204. После ги подрязах до 187 знака, за да сваля до 117,6 — и това беше
ГРЕШНАТА икономия: изчезнаха букви, които клиент може да напише. Сега азбуките
са ЦЕЛИ, всички подмножества стоят в хранилището (441,2 KB), а **джобът пази
само пакета, избран при сваляне**. Българинът тегли 204,1 KB веднъж; онлайн
липсваща азбука се дотегля при нужда. `unicode-range` върши останалото.

### 4б · Бързината като договор · `npm run merki`

Бюджетът живее в кода (`proba/merki.test.ts`) и при пробив връща червено.
Регресията се хваща от командата, не от усещането на собственика.

| мярка · 10 000 събития | измерено | бюджет |
| :---- | ---: | ---: |
| `fold` — цялото Огледало от нулата | **2,9 ms** | 20 ms |
| `smetki()` — месец с ДДС по акумулатори | **2,0 ms** | 10 ms |
| `proveriVerigata` — 10 000 звена SHA-256 | **82,0 ms** | 400 ms |
| append през Вратата (в паметта) | **61 938/с** | 500/с |
| студен старт: отваряне + четене от IndexedDB | **90,1 ms** | 300 ms |

**5/5 в зелено.** 10 000 събития са ≈ 10 години от живота на този бизнес.
Изводът стои: Огледалото се смята наново при всяко показване и това не струва
нищо — затова кеш и отметки (checkpoints) са ОТПАДНАЛИ като планирана работа.

**Числата са ПРЕБРОЕНИ, не оценени** — командите за преброяване са в умението
`/refresh`. Сгрешавани са два пъти в този проект, затова броенето е правило.

---

## 5 · Сверката · вход↔изход

Всяка сверка на веригата завършва тук, с таблица — **и когато разликата е нула.**
Проверената нула е различна от нулата, за която никой не е питал (правило 7).

### Сверка · 23.08.2026 · вечер · след презастрояването на описа

| мярка | стойност |
| :---- | ---: |
| обиколки до нула | **2** (правило 27 · втората върна 0) |
| звена проверени | **13 / 13** · с подзвената 5а и 8а — **15 проверки** |
| поправени | **6 файла** |
| архивирани | 0 · старите сверки минаха в `docs/otcheti/sverki-na-verigata.md` |
| числа, които се разминаваха | **2** |
| думи без тест | **10** (изброени в §3) |
| остава отворено | **3** — нито едно не спира работа (`00` §7) |

**Кое се разминаваше:**

| # | какво | къде | вярното |
| :---- | :---- | :---- | :---- |
| 1 | „20 резена", а скобата пропускаше 14в и 14г | `docs/09` §4а | **21 от 31**, преброени от `docs/03` |
| 2 | „деветте му прозореца" — „девет" идваше от МОЯ въпросник, който сдвояваше РАЗХОДИ/ПРИХОДИ и ОТЧЕТ/НОМЕНКЛАТУРИ | `docs/izvori/04` §2 · `docs/09` §2 | табовете в неговия файл са **единайсет** *(р48·[37])*; съвпадения по име с нашите шест екрана: **нула** |

**Кое беше следа от поправка в жив файл (правило 16):**

Двайсет и два реда „описът водеше X" и четири докладни блока стояха в
`docs/izvori/02-po-temi.md`. Живото е пренесено, следата е в
`docs/otcheti/prohod-17-koy-go-e-kazal.md`, живият опис е на чисто.

**Кое беше бездомен факт (правило 17):**

Шест негови изказвания от 23.08 живееха само в разговора — вписани в Журнала
като И79–И90. Четири от тях са ИЗБОР от меню и затова НЕ се цитират като негови
изречения (правило 21 · И84).

**Завършените сверки от по-рано** — 22.08 (резен 11) и 23.08 (резен 13) — са в
`docs/otcheti/sverki-na-verigata.md`. Тук стои само последната.

### Какво влезе в архива и защо

`docs/arhiv/` пази онова, което казва **какво се е случило ТОГАВА**. Живото в
тези файлове беше пренесено ПРЕДИ преместването, не след него. Индексът и
причината за всеки са в `arhiv/00-KAKVO-IMA-TUK.md` — обяснението стои **там**,
в архивирания файл. Живата папка не носи следи.

### Какво НЕ се пипа и защо

`ИЗВОР-1-ЧИСТ.md` (суровите думи). Неговите **475 302 знака** са котвата на
затворената сверка — редакция в него би счупила печата от §2 на `00`. Изворите
се **допълват** (`ИЗВОР-5`), не се редактират. Същото важи за целия архив.

---

*Верига на изворите · 22.08.2026. Ако нещо в §2 остане без тест — значи е*
*обещание. Този документ съществува, за да се вижда разликата.*
