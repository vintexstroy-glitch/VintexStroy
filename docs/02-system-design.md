# System Design · MasterBook · ядрото (път на запис) и прагът за носителя

**Дата:** 2026-08-22 · **Аудитория:** разработчик/агент · **Стъпва върху:** Архитектурния документ (не го дублира)

Тук е дълбокият дизайн на **фундамента** — Журналът и Вратата — плюс оценка на натоварването, която прави избора на носител лесен: дай едно число (наематели × записи/ден) и прочети реда в §4.

## 1 · Изисквания

**Функционални:** един вход за запис; дневник само за добавяне; производни изгледи; работа офлайн с моментална обратна връзка; много наематели с изолация; синхрон навън към Sheets.

**Нефункционални:** **нула загуба на данни** (над всичко); запис, който се вижда веднага (< 100 ms местно); ниска цена при малък мащаб; да оцелее до N наематели без пренаписване.

**Ограничения:** един разработчик/агент; Google екосистема (Drive/Sheets); **носителят е отворен** (ADR-001); BG локал; парите в цели стотинки.

## 2 · Високо ниво

Компонентите и потокът са в Архитектурния документ (Пясъчник → Врата → Журнал → Огледало → Мост). Тук фиксираме **договора**, който е еднакъв на трите носителя — това е, което позволява изборът да се отложи без цена.

**Storage избор по носител (един и същ договор отгоре):**

- **А · Sheets+GAS** — Журналът е лист „само добавяне"; Вратата е GAS `doPost`.
- **Б · свой сървър** — Журналът е таблица Postgres (append-only); Вратата е HTTP endpoint.
- **В · местно-първо** — Журналът е IndexedDB store; Вратата е локална функция; синхрон отложен.

## 3 · Deep dive · договорът на ядрото

### 3.1 Схема на събитието (една, за трите носителя)

```
Събитие {
  seq        : цяло, монотонно в рамките на наемател   // ред
  opId       : uuid на операцията                      // идемпотентност
  ts         : ISO време (една зона)
  наемател   : tenant_id                               // изолация
  actor      : имейл                                   // самоличност
  type       : 'НаемДобавен' | 'ПлащанеПрието' | 'Сторно' | ...
  entity     : {вид, id}                               // върху коя същност
  payload    : {…}                                     // само стотинки за пари
  prevHash   : хеш на предишното събитие
  hash       : hash(seq|opId|ts|tenant|type|entity|payload|prevHash)
}
```

Хеш-веригата прави всяко скрито редактиране откриваемо: `hash[n]` зависи от `prevHash = hash[n-1]`.

### 3.2 Врата · договор (единственият запис)

```
append(op) →
  1. провери ПРАВО (actor може ли да пише в този tenant/entity)   // Правата
  2. провери ВАЛИДНОСТ (схема, стотинки цели, задължителни полета)
  3. дедупликация по opId: ако вече съществува → върни същия резултат (идемпотентно)
  4. rev-предпазител: ако op.expectedRev ≠ текущия seq на entity → откажи (REPLAY, не STALE)
  5. append събитие (seq, prevHash, hash)
  6. върни { seq, hash }
```

Единичен писач на наемател (сериализация на стъпка 4–5) — иначе се къса веригата (инцидент №2/№1).

### 3.3 Огледало · проекция

```
състояние(entity) = fold(събития_за_entity, приложи)   // сторно се самопогасява
```

- **Отметки (checkpoints)** на всеки K събития ограничават цената на преизграждане.
- Отметката носи хеша при своя `seq`; при несъвпадение се **изхвърля мълчаливо** и се строи от нулата.
- Отметка НИКОГА не се ползва без проверка на хеша (иначе грешно състояние като истина — инцидент №5).

### 3.4 Мост · синхрон навън

```
за всеки наемател:
  cursor[tenant] = последен изнесен seq
  изнеси събития (seq > cursor) като sheet-delta, идемпотентно по opId
  cursor ← max(seq)
```

**Курсор на наемател, не общ** — общ курсор прескача събития (тих инцидент №4). Рефреш по поискване.

### 3.5 Грешки и повтаряне

- Клиентът пази неизпратените op-и (Пясъчник) и повтаря с нарастващо изчакване; `opId` прави повтарянето безопасно.
- При сблъсък (rev): върни транзакцията и препрати актуалния `seq` — без фалшив STALE.

## 4 · Scale · прагът, който решава носителя

Носителят не се избира по вкус, а по **число: наематели × записи/ден**. Долу — практичните тавани. (Числата са инженерна оценка на Google квотите към 2026; заковават се с реален тест.)

| носител | комфортен таван | твърда стена | латентност запис | цена |
| :---- | :---- | :---- | :---- | :---- |
| **А · Sheets+GAS** | ~1–3 наематели · до ~2–5 хил. записа/ден | GAS 6-мин изпълнение · ~20 хил. UrlFetch/ден · ~10M клетки/лист | 0.3–2 s (мрежа + GAS) | ~0 |
| **Б · свой сървър (Postgres)** | десетки–стотици наематели · милиони записи/ден | RAM/диск/връзки на инстанцията | 5–50 ms | сървър/месец |
| **В · местно-първо** | 1 потребител, неограничени местни записи | синхрон/устройство · без много-наематели | < 20 ms | ~0 |

**Как да четеш таблицата:**

- До няколко наематели и няколко хиляди записа/ден → **А** е достатъчен и почти без цена. Стената е GAS 6-минути (инцидент №3, лек: отметки) и дневната квота (инцидент №6).
- Премине ли се прагът (много наематели, десетки хиляди записа/ден) → **Б** за платещия голям клиент, докато евтиният остава на **А**. Тогава два носителя = условният „втори двигател", но **същият Журнал-договор** (opId+хеш) ги държи съвместими.
- Един потребител без нужда от облак → **В** е най-бърз; синхронът се добавя после.

**Нужно число от Иво (ADR-001):** *колко наематели очакваш през първата година и колко записа на ден прави най-натовареният?* С него редът се чете директно.

## 5 · Trade-offs (явно) и какво да преразгледаме при растеж

- **Отложен носител** · + нула цена за отлагане, преносимост; − малко повече дисциплина да не изтече специфика на носителя нагоре. **Струва си**, докато Вратата и Журналът са единствената връзка.
- **Хеш-верига** · + откриваемост на всяка намеса; − цена на изчисление при запис. При Б може да е фонова; при А върви в GAS. **Струва си** — тя направи одита на загубата възможен.
- **Отметки** · + ограничават преизграждането; − още едно нещо, което може да е развалено (лек: винаги с проверка на хеша, изхвърляеми). **Струва си** при >няколко хиляди събития на изглед.
- **Единичен писач на наемател** · + пази веригата; − сериализира записа. При А това е даденост; при Б се прави с ред/ключалка. **Струва си** — алтернативата е скъсана верига.

**Преразгледай при растеж:** (1) прагът А→Б, щом най-натовареният наемател мине ~половината GAS квота; (2) отметки на изглед, щом преизграждането надхвърли ~1 s; (3) курсор на наемател — още от ден 1, не после.

---

*System design · Етап 1 нов · 22.08.2026. Ядрото е носител-агностично по замисъл; §4 прави избора на носител въпрос на едно число. Свързани: Архитектурен документ · файл 3 · План за реакция.*
