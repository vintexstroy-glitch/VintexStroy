import { defineConfig } from 'vite';

export default defineConfig({
  /**
   * ОТНОСИТЕЛНИ ПЪТИЩА · за да върви и от ПОДПАПКА.
   *
   * GitHub Pages сервира проекта на `…/VintexStroy/`, не в корена. Заковано
   * `/`, всеки път към `assets/` щеше да сочи корена на домейна — тоест
   * приложението щеше да строи адреси, които не съществуват, и да показва
   * бял екран без нито едно съобщение.
   *
   * `'./'` работи И на двете места: в корена на свой домейн и в подпапка.
   * Целият код вече пише пътищата си относително (`./sw.js`, `./stil.css`),
   * затова тук няма изключение, което да се помни.
   */
  base: './',
  root: 'app',
  server: { port: 5173, open: false },
  build: { outDir: '../dist', emptyOutDir: true },
});
