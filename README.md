# VintexStroy · MasterBook

Приложение за управление на имоти, наеми, продажби, кредити, счетоводство,
плащания и проекти (Гант) — за един собственик с интензивен график.

**Издание: Стартъп Алфа** — изцяло работеща HTML версия с непълна
функционалност. Редът е Алфа → Бета → приложенията за десктоп и телефон, които
идват СЛЕД HTML версията. „Издание" е докъде е стигнал кодът; „план" е какво
може акаунтът (матрицата на ADR-007) — двете не се сливат.

**Местно-първо. Нула чужди зависимости в готовия пакет.** Журналът живее в
браузъра на собственика; няма сървър и няма мрежа.

## Какво има тук

```
src/yadro/          ЯДРОТО · Журнал + Врата (носител-агностично)
  pari.ts           цели стотинки, разпределяне без загуба
  data.ts           истински календарни дни — 31 февруари не минава
  sabitie.ts        схемата на събитието — договорът
  hash.ts           хеш-верига + проверка на веригата
  kotva.ts          котва срещу скъсяване отзад
  dnevnik.ts        Журналът (порт + реализация в паметта)
  pravata.ts        порт за правата: кой пише и кой сваля
  samolichnost.ts   вход БЕЗ ПАРОЛА · Google · Microsoft · Apple · passkey
  vrata.ts          единственият вход за запис + спирателен кран
  sverka.ts         сверка вход↔изход
src/domein/         домейнът · събития, действия, ДДС, сметки, замразяване, планове
  butoni.ts         бутоните са МОДЕЛИ НА ПЪТИЩА · десетте пътя, изброени
  chisla.ts         знакът · Приход /+/ и Разход /−/ · умение на данните
src/ogledalo/       ОГЛЕДАЛОТО · производно състояние, fold(събития)
src/iztochnik/      четци: CSV · Excel · PDF · снимка на файл (без библиотеки)
  model.ts          картата на хедъра — приложението пита ВЕДНЪЖ и помни
src/iznos/          писач на .xlsx с филтри (без библиотеки)
src/nositel/        Журналът в IndexedDB, хеш за браузър и за Node, хранилище
src/migratsiya/     внасяне на стари регистри — със сверка ПРЕДИ записа
app/                екраните · Имоти · Пари · Стойност на Състояние · Управление ·
                    Сметки · Настройки · ИИ · Табове · Табло
tests/              инвариантите, домейнът, фъртуната, имената
proba/              проход през истински браузър + мерки под договор
docs/               решенията · docs/arhiv/ — приключеното
  izvori/           ДУМИТЕ му: чистият извор (журнал) + по теми (огледало)
stroezh/            строежът: печат на джоба, икони
sql/                заявките към базата на думите (dumite.db)
design/             платното за скиците на екраните
.claude/skills/     умения: masterbook-data · matematika · refresh · doklad · ogledala.sh
CLAUDE.md           правилата, които не се нарушават
```

## Пускане

```bash
npm install
npm run dev        # отваря екрана на http://localhost:5173

npm run typecheck  # типове
npm test           # тестовете
npm run proba      # проход през истински браузър
npm run merki      # бързината под договор
```

Данните живеят в IndexedDB на твоя браузър. Затвориш ли раздела, всичко си стои;
отвориш ли го другаде, е празно. Бутонът **Изнеси Журнала** сваля всичко като
`.json`, а **Архив за Ексел** — пет листа с готови филтри.

## Откъде се чете

`docs/00-CHETI-PARVO.md` е входната врата: там е картата кой файл какво решава,
печатът на изворите и какво чака дума. Тук не се преписва.

Думите на собственика — суровите и подредените по теми — са в `docs/izvori/`.

## Докъде е стигнато

Работещо приложение: имоти и наеми (с прекратяване, поправка и сторно), пари
(месечно начисляване, частични плащания), сметки (ДДС от общата цена, двете
страни, справка която **заключва** периода), източници (Excel · CSV · PDF се
ЧЕТАТ, не се качват), архив за Ексел с фини филтри, табло с вход без парола, и
офлайн джоб, който работи с прекъсната мрежа.

Непозната таблица не се гади: приложението **пита веднъж** коя колона какво е,
записва картата в Журнала, и вторият файл със същата глава минава сам. Ставката
на ДДС идва от реда, а сверката „фактури ↔ извлечения ↔ внесено" казва не само
колко се разминава, а КЪДЕ (ADR-009).

Пътищата са **бутони**, правени в Настройки: име, папка, действие, посока и кои
хедъри приемат. Един бутон гълта няколко файла наведнъж и ги сверява като една
партида; чужд файл се отказва на глас. Всяка числова колона се появява сама в
**Приход /+/** или **Разход /−/** според сбора си, а сверката влиза в Журнала —
и когато разликата е нула (ADR-010).

Колоната има три състояния — данни, формула и празна: формулната се смята от
други колони, ражда се затворена и се мени само от Стопанина (ADR-025).
Табовете са списък секции — стационарните екрани приемат още секции, а
изборът в едната секция стеснява вързаната за нея (ADR-027). Колони с еднакъв
номер са свързани — номерът е ВРЪЗКА, не адрес, и се мести с колоната (ADR-028).

Агентът има табло: карта, протокол, закони и входяща кутия — той чете, смята и
ПРЕДЛАГА, а записва човекът (ADR-026). Протоколът му е непроменим след
създаване: нужна ли е промяна, агентът се закрива и се прави нов. Задачите му
носят разписание — всекидневно, седмично, за срок или ПОСТОЯННО (норма, която
не се изключва) — и всяко пускане, всяко умение и всяка задача минават през код
от писмо: честна спирачка, не ключалка (ADR-029). А онова, което чете, е месецът
като **таблица** — потоци, акумулатори и показатели, с предходния месец и
разликата, и със сверките до тях; имена на наематели и доставчици не напускат
устройството, и екранът показва дословно какво излиза (ADR-030).

**Числата — преброени, не оценени — са в `docs/09` §4.** Тук нарочно не се
преписват: преписаното число рано или късно се разминава.

## Основни правила

Живеят на ЕДНО място: **`CLAUDE.md`**. Тук нарочно не се преписват — нито
текстът им, нито БРОЯТ им. Преписаното правило се разминава точно като
преписаното число, и точно това стана тук: докато правилата станаха повече,
този ред още твърдеше колко са.

## Сверка на веригата

`/refresh` минава цялата верига — от изворите в Драйва до последния файл тук —
и поправя разминатото на чисто, а приключеното мести в `docs/arhiv/`. Умението
е в `.claude/skills/refresh/`.

## Още не е в хранилището

- `dumite.db` (SQLite, ~3.5 MB) — стои в Google Drive
- изворите `ИЗВОР-1` … `ИЗВОР-5` — стоят в Google Drive, само за четене

Двоични файлове от този размер не са за Git история. Виж `docs/00-CHETI-PARVO.md`
за това кои версии са валидни.
